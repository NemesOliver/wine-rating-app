import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      light: "#780000",
      main: "#AC0000",
      dark: "#bc3333",
    },
    secondary: {
      main: "#ff6e40",
    },
  },
});
